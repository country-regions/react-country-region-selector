describe('rcrs: Rcrs component', () => {
	// beforeEach(() => cy.visit('/iframe.html?id=rcrs--primary'));
	//
	// it('should render the component', () => {
	// 	cy.get('h1').should('contain', 'Welcome to Rcrs!');
	// });
});
